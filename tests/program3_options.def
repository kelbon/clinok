DECLARE_STRING_ENUM(log_level_e, trace, debug, info, warn, error)
OPTION(log_level_e, log_level, "Verbosity level", default("trace"))

INTEGER(timeout, "Request timeout in seconds", default("10"))
STRING(user, "User name")
OPTION(Point, location, "Custom type: point x,y", default("0", "0"))

RENAME(log_level, "log-level")

ALIAS(l, log-level)
ALIAS(t, timeout)
