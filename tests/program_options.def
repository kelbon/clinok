TAG(mytag, "something")
BOOLEAN(works, "it works", default("false"))
STRING(hello_world, "because, why not",    
default("hello, man"))
STRING(myname,
       R"(fdsfdsfsddsfsdffdsfsireriteowireowroiewr)",   default(""))
STRING(ABC2,
       "VERY"
       "mda\n",
       default("why"))

INTEGER(myint, "myint description", default("17"))
INTEGER(myint2, "myint2 without default")
DECLARE_STRING_ENUM(color_e,  red,               green, blue              ,yellow)
OPTION(color_e, color, "description")
ALIAS(h, help)
ALIAS(w, works)
ALIAS(hh, myint)
ALIAS(i, myint2)
ALIAS(c, color)
