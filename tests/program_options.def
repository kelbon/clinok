TAG(mytag, "something")
BOOLEAN(works, "it works", default(false))
STRING(hello_world, "because, why not", default("hello, man"))
STRING(myname,
       R"(fdsfdsfsddsfsdffdsfsireriteowireowroiewr)")
STRING(ABC2,
       "VERY"
       "mda\n",
       default("why"))
INTEGER(ab, "integer description", default(15))
ENUM(abc, "enum description", 1, 2, 3, 4, 5)
ENUM(str_enum, "enum str description", "hello", "world", "float")
